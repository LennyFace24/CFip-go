# CFip优选ip go版本

## 如何使用

新建 ip.txt，逐行写入ip，支持cidr格式以及限定采样数

```
1.1.1.1
1.1.1.1/13
1.1.1.1/13=500
```


```go
go run .
```

运行后可在日志看到优选ip列表

## TUI 使用

实时可视化测速进度与结果表格：

```bash
go run ./cmd/tui
```

按键：`↑/↓` 导航，`s` 切换排序（延迟/IP/状态），`/` 筛选 IP 子串，`r` 重新测速，`q` 退出。